# BOTTOM-001 实施计划

## 1. 因果状态模型

- 新建 `research/bottom_state.py`。
- 以 OHLCV 和已对齐的结构、供需、大盘证据为输入。
- 输出状态、原始状态、总分、五个分项、覆盖率、记忆年龄、迁移标志、失效位、支持证据与反证。
- 用前缀不变测试防止未来数据泄漏。

## 2. Web 服务接入

- 新建 `web/services/bottom_state.py`，把图表行转换为模型证据并按日期写回。
- 在供需与市场状态完成后调用，确保模型能读取完整上下文。
- 缺少数据时输出类型明确的 `unavailable`，不让页面报错。

## 3. 统一模型输出

- 注册 `bottoming_reversal_state_v1`，归入向上结构模型组。
- 权限保持 `advisory`，状态生命周期标记为 `research`。
- 展示分项、状态年龄、失效位、支持证据与反证。

## 4. 图表和详情 UI

- 新增筑底状态图层和状态迁移标记。
- 摘要栏新增当前筑底状态徽标。
- 日期详情新增状态、分数、年龄、失效位。
- 增加中英文名称、解释、限制、状态和条件翻译。

## 5. 验证与 TODO

- 运行模型、服务、模型输出、前端资源与 API 定向测试。
- 运行完整测试集。
- 将 TODO 中实现项标记完成，样本外验证保持待办。
